// Operador de String (String Operator)

// comparação (comparison)
// console.log('a' == 'a')

// Concatenação (concatenation)
  // Retorna a união de duas strings
  console.log('a' + 'b')

  let alpha = "alpha"

  alpha += 'bet' + 's'

  console.log(alpha += 354)