// Operadores de comparação

// Irá comparar valores e retornar um Boolean como resposta à comparação

let one = 1
let two = 2

// os operadores == significam igual a
  /*console.log(two == 1)
  console.log(one == 1)
  console.log(two == 2)*/

  // Os operadores != significam diferente de
    /*console.log(two != 1)
    console.log(one != 1)
    console.log(two != one)*/

  // Os operadores === significam estritamente igual a, ou seja, o valor e o tipo deverão ser o mesmo
   /*console.log(two === '2')
    console.log(two === 2)
    console.log(one === 1)
    console.log(one === two)*/

  // Os operadores !== significam estritamente diferente de, ou seja, o valor e o tipo deverão ser diferente
  /*console.log(two !== "2")
  console.log(two !== 2)
  console.log(two !== one)*/

  // > maior que
    console.log(two > one)
    console.log(two > 3)

  // < menor que 
    console.log(two < 2)
    console.log(two < 5)

  // >= maior ou igual a
    console.log(one >= 1)
    console.log(two >= one)
    console.log(two >= 4)
  // <= menor ou igual a
    console.log(two <= 2)
    console.log(one <= two)
    console.log(5 <= one)