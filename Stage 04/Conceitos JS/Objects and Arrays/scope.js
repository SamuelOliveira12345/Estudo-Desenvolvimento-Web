/*
# Escopo

 * O escopo determina a visibilidade de alguma variavel no JS
*/

/*
# Block statement (Declaração de bloco)

 * Iniciando um bloco
  {
    Aqui é um bloco onde é possível adicionar qualquer codigo
  } fechando o codigo

*/

/*
# O bloco tambem íra criar um novo escopo, chamado de `Block-scoped`
*/

/*
  # O var funciona tanto num escopo a nível global quanto local, ou seja, podemos alterar variáveis tanto dentro quanto fora de um escopo

  # let e const só funcionam a nível local, ou seja, só pode ser alterada em um escopo local, isso quer dizer que não é possível utilizar esse tipo de variável de fora do escopo ao qual ele está definido.
*/

