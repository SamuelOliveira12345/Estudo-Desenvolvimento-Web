// 1. Declare uma variavel de nome weight.

  let weight = 105

// 2. Que tipo de dado é a variavel acima?

  console.log(typeof weight)

/* 3. Declare uma variavel e atribua valores para cada um dos dados:
    * name: String
    * age: Number (integer)
    * weight: Number (float)
    * isSubscribed: Boolean
*/    

  const person = {
    name: 'Samuel',
    age: 28,
    weight: 105.4,
    isSubscribed: true
  }

/* 4. A variável student abaixo é de que tipo de dados?

    let student = {);
      
      R: Object

   4.1. Atribua a ela as mesmas propriedades  e valores do exercicio 3.
  
    let student = {
    name: "Samuel",
    age: 28,
    weight: 105.4,
    isSubscribed: true
    };

   4.2. Mostre no console a seguinte mensagem:
    <name> tem <age> de idade e pesa <weight> kg

    console.log(`${student.name} tem ${student.age} anos de idade e pesa ${student.weight} kg`)
*/
 // 5. Declare uma variável do tipo Array, de nome students e atribnua a ela nenhum valor, ou seja, somente o Array vazio

 let students = []

 // 6. Atribua valor para a variável acima, colocando dentro dela o objeto student da questão 4. (Não copiar e colar o objeto, mas usar o objeto criado e inserir ele no Array)

 students = [
  student
 ]

 // 7. Coloque no console o valor da posição zero do Array acima

 console.log(students[0])

 // 8. Crie um novo student e coloque na posição 1 do Array students

 const student2 = {
  name: "samuel",
  age: 28,
  weight: 105.5,
  isSubscribed: true
 }

 students = [
  student,
  student2
 ]

 // 9. Sem rodar o cófigo responda qual é a resposta do código abaixo e por que? Após sua resposta rode o código e veja se você acertou.

 console.log(a)
 var a=1
 