  var name

  name = "Samuel"

// Agrupamento de declaração (statement grouping)
  let age, isHuman

  age = 28
  isHuman = true

  // multiplos argumentos na função
  console.log(name, age, isHuman)

  // As variáveis agrupadas devem ser separadas por virgulas quando forem exibida de forma agrupada

  // escrita de texto mais variaveis 
  console.log('O' + name + 'tem' + age + 'anos.')

  // É importante concatenar os valores, ou seja, juntar a sting com as variaveis e para isso é utilizado o "+"

  //interpolação com template literals or template strings, fazendo uso de crase ``
  console.log(`O ${name} tem ${age} anos.`)